{
  "name": "backlink-prospect-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Local backlink prospect manager that imports SEMrush CSV exports and syncs qualified prospects to Google Sheets and Notion.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "npm run install:all",
    "install:all": "npm install --include=dev --prefix backend && npm install --include=dev --prefix frontend",
    "dev": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  }
}
